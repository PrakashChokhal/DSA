class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        
        return true;
        // Since all elements in nums1 are distinct, it is always possible to make all elements in nums2 odd, so the function should always return true.
    }
};